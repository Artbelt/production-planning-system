# Yum - Счетчик калорий

Веб-приложение для подсчета калорий с поддержкой офлайн-режима и локального хранения данных.

## 🚀 Возможности MVP

- ✅ Быстрый ввод еды через поиск или текстовый ввод
- ✅ Подсчет калорий за день
- ✅ Локальное хранение данных (IndexedDB) для офлайн-режима
- ✅ Отображение суточной нормы калорий с прогресс-баром
- ✅ История по дням
- ✅ Настраиваемая суточная норма калорий

## 📋 Требования

- Node.js 18+ 
- npm или yarn

## 🛠 Установка

### Backend

```bash
cd backend
npm install
```

### Frontend

```bash
cd frontend
npm install
```

## 🏃 Запуск

### Запуск Backend

```bash
cd backend
npm run dev
```

Сервер запустится на `http://localhost:3001`

### Запуск Frontend

В новом терминале:

```bash
cd frontend
npm run dev
```

Приложение откроется на `http://localhost:3000`

## 📁 Структура проекта

```
Yum/
├── frontend/              # React + TypeScript + Vite
│   ├── src/
│   │   ├── components/    # UI компоненты
│   │   ├── stores/        # Zustand stores
│   │   ├── services/      # API сервисы
│   │   ├── db/            # IndexedDB обертка
│   │   ├── types/         # TypeScript типы
│   │   └── App.tsx
│   └── package.json
├── backend/               # Express + TypeScript
│   ├── src/
│   │   ├── routes/        # API роуты
│   │   ├── models/        # Модели данных
│   │   ├── db/            # SQLite подключение
│   │   └── server.ts
│   └── package.json
└── README.md
```

## 🔌 API Endpoints

### Продукты

- `GET /api/foods?q=query&limit=20` - поиск продуктов
- `POST /api/foods` - создание продукта
- `GET /api/foods/:id` - получение продукта по ID

### Дневные логи

- `GET /api/daily-logs/:date` - получение лога за дату (YYYY-MM-DD)
- `POST /api/daily-logs` - создание записи
- `DELETE /api/daily-logs/:id` - удаление записи
- `GET /api/daily-logs/history/dates` - список дат с записями

### Настройки пользователя

- `GET /api/user/settings` - получение настроек
- `PUT /api/user/settings` - обновление настроек

## 💾 База данных

### Backend (SQLite)

База данных создается автоматически при первом запуске в `backend/data/yum.db`.

Таблицы:
- `foods` - справочник продуктов
- `daily_logs` - записи о приеме пищи
- `user_settings` - настройки пользователя

### Frontend (IndexedDB)

Локальная база данных для офлайн-режима:
- `foods` - кэш продуктов
- `daily_logs` - локальные записи
- `user_settings` - локальные настройки
- `sync_queue` - очередь синхронизации (для будущего расширения)

## 🎨 Особенности

- **Офлайн-режим**: Все операции сначала сохраняются локально, затем синхронизируются с бэкендом
- **Быстрый ввод**: Автодополнение и поиск продуктов в реальном времени
- **Простой UI**: Крупные цифры, минимальный текст, фокус на скорости использования
- **Типизация**: Полная типизация TypeScript для безопасности кода

## 📝 Использование

1. **Добавление еды**: Введите название продукта в поле поиска. Если продукт найден, выберите его из списка. Если нет - можно создать новый, указав калории (например: "яблоко 52").

2. **Просмотр калорий**: Текущее количество калорий и прогресс к цели отображаются в верхней части экрана.

3. **История**: Переключитесь на вкладку "История" для просмотра предыдущих дней.

4. **Настройки**: Суточная норма калорий настраивается через API (в будущих версиях будет UI).

## 🔮 Планы развития

См. [ROADMAP.md](ROADMAP.md) для детального плана развития проекта.

## 📄 Лицензия

MIT
