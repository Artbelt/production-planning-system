# Руководство по настройке автоматического резервного копирования

## 📦 Что включено

1. **backup_db.bat** - Batch скрипт для Windows (рекомендуется)
2. **backup_db.php** - PHP скрипт (альтернативный вариант)
3. Инструкции по настройке планировщика задач Windows

## 🚀 Вариант 1: Использование Batch скрипта (рекомендуется)

### Шаг 1: Настройте скрипт

Откройте файл `backup_db.bat` и при необходимости измените настройки:

```batch
set MYSQL_DIR=C:\xampp\mysql\bin        # Путь к MySQL
set BACKUP_DIR=C:\xampp\backups\plan_u5 # Директория для резервных копий
set DB_NAME=plan_u5                      # Имя базы данных
set DB_USER=root                         # Пользователь MySQL
set DB_PASS=                             # Пароль MySQL (оставьте пустым если нет пароля)
```

### Шаг 2: Проверьте работу скрипта вручную

Дважды кликните на `backup_db.bat` или запустите из командной строки:

```cmd
cd C:\xampp\htdocs\plan_U5
backup_db.bat
```

Проверьте, что резервная копия создалась в папке `C:\xampp\backups\plan_u5\`

### Шаг 3: Настройте автоматический запуск через Планировщик задач Windows

1. **Откройте Планировщик задач:**
   - Нажмите `Win + R`
   - Введите `taskschd.msc` и нажмите Enter

2. **Создайте новую задачу:**
   - В правой панели нажмите "Создать задачу"
   - На вкладке "Общие":
     - Имя: `Резервное копирование БД plan_u5`
     - Описание: `Автоматическое резервное копирование базы данных plan_u5`
     - Отметить: "Выполнять с наивысшими правами"

3. **Настройте триггер (когда запускать):**
   - Перейдите на вкладку "Триггеры"
   - Нажмите "Создать"
   - Начало задачи: "По расписанию"
   - Параметры: "Ежедневно"
   - Время: выберите удобное время (например, 02:00 ночи)
   - Повторять задачу: можно настроить (например, каждые 6 часов)
   - Нажмите "OK"

4. **Настройте действие (что выполнять):**
   - Перейдите на вкладку "Действия"
   - Нажмите "Создать"
   - Действие: "Запуск программы"
   - Программа или сценарий: `C:\xampp\htdocs\plan_U5\backup_db.bat`
   - Рабочая папка: `C:\xampp\htdocs\plan_U5`
   - Нажмите "OK"

5. **Настройте условия (опционально):**
   - Перейдите на вкладку "Условия"
   - Снимите галочку "Запускать только при питании от электросети" (если нужно запускать на ноутбуке)
   - Можно включить "Пробуждать компьютер для выполнения задачи"

6. **Настройте параметры:**
   - Перейдите на вкладку "Параметры"
   - Включите: "Выполнять задачу немедленно, если пропущен плановый запуск"
   - Включите: "При сбое запускать задачу повторно через: 10 минут"

7. **Сохраните задачу:**
   - Нажмите "OK"
   - Введите пароль администратора, если потребуется

### Шаг 4: Проверьте выполнение

- Дождитесь времени запуска или запустите задачу вручную (правый клик → "Выполнить")
- Проверьте папку `C:\xampp\backups\plan_u5\` - должны появиться файлы резервных копий
- Проверьте журнал задач в Планировщике задач

## 🔧 Вариант 2: Использование PHP скрипта

### Шаг 1: Настройте скрипт

Откройте файл `backup_db.php` и при необходимости измените настройки в начале файла.

### Шаг 2: Создайте задачу в Планировщике задач

1. Создайте новую задачу (как в Варианте 1)
2. В действии укажите:
   - Программа: `C:\xampp\php\php.exe`
   - Аргументы: `C:\xampp\htdocs\plan_U5\backup_db.php`
   - Рабочая папка: `C:\xampp\htdocs\plan_U5`

## 📊 Мониторинг резервных копий

### Просмотр логов

PHP скрипт создает лог файл: `C:\xampp\backups\plan_u5\backup_log.txt`

### Проверка резервных копий через SQL

```sql
-- Посмотреть размер всех резервных копий в таблице
SELECT 
    COUNT(*) as total_backups,
    MIN(backup_time) as oldest_backup,
    MAX(backup_time) as newest_backup
FROM salon_filter_structure_backup;
```

### Проверка файлов резервных копий

```powershell
# В PowerShell
Get-ChildItem C:\xampp\backups\plan_u5\ | Sort-Object LastWriteTime -Descending | Select-Object -First 10 Name, Length, LastWriteTime
```

## 🔄 Восстановление из резервной копии

### Восстановление из SQL файла:

```cmd
# В командной строке
cd C:\xampp\mysql\bin
mysql.exe -u root plan_u5 < C:\xampp\backups\plan_u5\plan_u5_20240101_020000.sql
```

### Восстановление через phpMyAdmin:

1. Откройте phpMyAdmin
2. Выберите базу данных `plan_u5`
3. Перейдите на вкладку "Импорт"
4. Выберите файл резервной копии
5. Нажмите "Вперед"

## ⚙️ Дополнительные настройки

### Изменение частоты резервного копирования

В Планировщике задач измените триггер:
- Ежедневно в определенное время
- Каждые N часов
- Еженедельно
- И т.д.

### Хранение резервных копий на другом диске

Измените `BACKUP_DIR` в скрипте на путь к другому диску:
```batch
set BACKUP_DIR=D:\backups\plan_u5
```

### Автоматическая отправка резервных копий по email

Можно добавить в скрипт отправку файла по email после создания резервной копии.

### Синхронизация с облачным хранилищем

Можно настроить синхронизацию папки с резервными копиями с:
- Google Drive
- OneDrive
- Dropbox
- Или другим облачным хранилищем

## 🛡️ Рекомендации по безопасности

1. **Храните резервные копии на отдельном диске** - защита от сбоя основного диска
2. **Храните копии вне сервера** - защита от пожара, кражи и т.д.
3. **Шифруйте резервные копии** - если они содержат конфиденциальные данные
4. **Проверяйте резервные копии** - периодически проверяйте, что они восстанавливаются
5. **Ведите журнал** - отслеживайте успешность создания резервных копий

## 📝 Проверка работы резервного копирования

Создайте тестовую задачу для проверки:

```batch
REM test_backup.bat
@echo off
echo Тест резервного копирования...
call backup_db.bat
if %ERRORLEVEL% EQU 0 (
    echo Тест пройден успешно!
) else (
    echo Тест провален!
)
pause
```

## ❓ Решение проблем

### Проблема: Резервная копия не создается

**Решение:**
1. Проверьте права доступа к папке `C:\xampp\backups\plan_u5\`
2. Проверьте, что MySQL запущен
3. Проверьте логи в Планировщике задач
4. Запустите скрипт вручную и проверьте ошибки

### Проблема: Задача не запускается автоматически

**Решение:**
1. Проверьте, что задача включена в Планировщике задач
2. Проверьте триггеры задачи
3. Проверьте права пользователя
4. Проверьте журнал задач Windows

### Проблема: Недостаточно места на диске

**Решение:**
1. Уменьшите срок хранения резервных копий (измените `keep_days`)
2. Включите сжатие резервных копий (установите 7-Zip)
3. Очистите старые резервные копии вручную

## 📞 Полезные команды

### Просмотр последних резервных копий:
```powershell
Get-ChildItem C:\xampp\backups\plan_u5\ | Sort-Object LastWriteTime -Descending | Select-Object -First 5
```

### Удаление старых резервных копий вручную:
```powershell
Get-ChildItem C:\xampp\backups\plan_u5\ | Where-Object {$_.LastWriteTime -lt (Get-Date).AddDays(-30)} | Remove-Item
```

### Проверка размера всех резервных копий:
```powershell
$totalSize = (Get-ChildItem C:\xampp\backups\plan_u5\ | Measure-Object -Property Length -Sum).Sum
Write-Host "Общий размер резервных копий: $([math]::Round($totalSize/1GB, 2)) GB"
```



