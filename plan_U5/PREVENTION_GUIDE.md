# Руководство по предотвращению потери данных

## ✅ Что уже сделано для защиты

### 1. Исправлен код обновления (`processing_edit_filter_properties.php`)

**Изменения:**
- Используется `COALESCE(NULLIF(?, ''), поле)` в SQL - пустые значения не перезаписывают существующие данные
- Автоматическое создание резервных копий перед каждым обновлением
- Логирование изменений критических полей

**Как это работает:**
```sql
-- Вместо простого обновления:
UPDATE salon_filter_structure SET box = '' WHERE filter = '...'

-- Теперь используется:
UPDATE salon_filter_structure SET box = COALESCE(NULLIF(?, ''), box) WHERE filter = '...'
-- Это означает: если новое значение пустое, оставить старое
```

### 2. Автоматическое резервное копирование

Перед каждым обновлением создается резервная копия в таблице `salon_filter_structure_backup`.

**Восстановление из резервной копии:**
```sql
-- Посмотреть все резервные копии для фильтра
SELECT * FROM salon_filter_structure_backup 
WHERE filter = 'НАЗВАНИЕ_ФИЛЬТРА' 
ORDER BY backup_time DESC;

-- Восстановить из последней резервной копии
-- (используйте скрипт restore_from_backup.php)
```

### 3. Логирование изменений

Все изменения критических полей логируются в таблицу `salon_filter_structure_change_log`.

**Просмотр логов:**
```sql
-- Посмотреть все очистки полей
SELECT * FROM salon_filter_structure_change_log 
WHERE change_type = 'CLEARED' 
ORDER BY change_time DESC;

-- Посмотреть изменения конкретного фильтра
SELECT * FROM salon_filter_structure_change_log 
WHERE filter_name = 'НАЗВАНИЕ_ФИЛЬТРА' 
ORDER BY change_time DESC;
```

## 🔧 Дополнительные шаги защиты

### Шаг 1: Установите SQL триггеры

Выполните SQL скрипт для создания триггеров защиты:

```bash
# В командной строке MySQL:
mysql -u root -p plan_u5 < create_protection_triggers.sql

# Или через phpMyAdmin:
# Откройте create_protection_triggers.sql и выполните его содержимое
```

Триггеры будут автоматически логировать все попытки очистки критических полей.

### Шаг 2: Настройте регулярные резервные копии БД

**Вариант 1: Через планировщик задач Windows**

Создайте файл `backup_db.bat`:
```batch
@echo off
set BACKUP_DIR=C:\xampp\backups
set DATE=%date:~-4,4%%date:~-7,2%%date:~-10,2%
set TIME=%time:~0,2%%time:~3,2%%time:~6,2%
set TIME=%TIME: =0%

mkdir "%BACKUP_DIR%" 2>nul

"C:\xampp\mysql\bin\mysqldump.exe" -u root plan_u5 > "%BACKUP_DIR%\plan_u5_%DATE%_%TIME%.sql"

REM Удаляем резервные копии старше 30 дней
forfiles /p "%BACKUP_DIR%" /m *.sql /d -30 /c "cmd /c del @path"
```

Добавьте в планировщик задач Windows для ежедневного выполнения.

**Вариант 2: Через PHP скрипт**

Создайте `cron_backup.php` и настройте его выполнение через cron или планировщик задач.

### Шаг 3: Включите Binary Log MySQL

Binary log позволяет восстановить данные из истории изменений:

```sql
-- Включить binary log
SET GLOBAL log_bin = 'ON';

-- Проверить статус
SHOW VARIABLES LIKE 'log_bin';
```

### Шаг 4: Настройте мониторинг

Создайте скрипт для проверки целостности данных:

```php
// check_data_integrity.php
// Запускайте его периодически для проверки пустых полей
```

## 📋 Чеклист защиты данных

- [x] Исправлен код обновления (использует COALESCE)
- [x] Добавлено автоматическое резервное копирование перед обновлением
- [x] Добавлено логирование изменений
- [ ] Установлены SQL триггеры защиты
- [ ] Настроены регулярные резервные копии БД
- [ ] Включен Binary Log MySQL
- [ ] Настроен мониторинг целостности данных

## 🚨 Что делать при обнаружении проблемы

1. **Немедленно проверьте резервные копии:**
   ```sql
   SELECT * FROM salon_filter_structure_backup 
   WHERE filter = 'ПРОБЛЕМНЫЙ_ФИЛЬТР' 
   ORDER BY backup_time DESC LIMIT 5;
   ```

2. **Проверьте логи изменений:**
   ```sql
   SELECT * FROM salon_filter_structure_change_log 
   WHERE filter_name = 'ПРОБЛЕМНЫЙ_ФИЛЬТР' 
   AND change_type = 'CLEARED'
   ORDER BY change_time DESC;
   ```

3. **Восстановите из резервной копии:**
   - Используйте скрипт `restore_from_backup.php`
   - Или выполните SQL вручную

4. **Проверьте логи веб-сервера:**
   - Кто и когда делал запросы к `processing_edit_filter_properties.php`
   - Используйте `analyze_logs.php` для анализа

## 📞 Полезные команды

**Проверка текущего состояния:**
```sql
-- Сколько записей с пустыми полями
SELECT 
    COUNT(*) as total,
    SUM(CASE WHEN box IS NULL OR box = '' THEN 1 ELSE 0 END) as empty_box,
    SUM(CASE WHEN insertion_count IS NULL OR insertion_count = '' THEN 1 ELSE 0 END) as empty_insertion,
    SUM(CASE WHEN g_box IS NULL OR g_box = '' THEN 1 ELSE 0 END) as empty_gbox
FROM salon_filter_structure;
```

**Просмотр последних изменений:**
```sql
SELECT * FROM salon_filter_structure_change_log 
ORDER BY change_time DESC 
LIMIT 20;
```

**Очистка старых резервных копий:**
```sql
-- Удалить резервные копии старше 30 дней
DELETE FROM salon_filter_structure_backup 
WHERE backup_time < DATE_SUB(NOW(), INTERVAL 30 DAY);
```




